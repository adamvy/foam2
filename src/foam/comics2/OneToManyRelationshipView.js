foam.CLASS({
  package: 'foam.comics2',
  name: 'OneToManyRelationshipView',
  extends: 'foam.u2.View',
  requires: [
    'foam.comics2.DAOControllerView'
  ],
  methods: [
    function initE() {
      this.
        setNodeName('div').
        callIf(this.
        start(this.DAOControllerView, {
        }
    }
  ]
});
